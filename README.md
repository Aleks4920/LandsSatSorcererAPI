# LandsSatSorcererAPI

Python API for working with Landsat satellite imagery and data.

## Structure

api.py implements the API. requirements.txt lists the Python dependencies. startup.sh is a startup and deployment script.

## Usage

Install dependencies with pip install -r requirements.txt, then run api.py, or use startup.sh to start the service.
